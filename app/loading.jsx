export default function Loading() {
    return (
      <main className="text-center">  
        <h2 className="text-primary">Procesando...</h2>
        <p>Esperemos que antes de final de año </p>
      </main>
    )
  }